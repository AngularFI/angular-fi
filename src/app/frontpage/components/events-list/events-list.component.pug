.events(*ngIf="events && events.length > 0")
  ul(*ngFor="let event of events")
    li
      a(href="{{event.link}}") {{event.name}}
      | , {{event.time | date:'DD.MM.YYYY'}}

p(*ngIf="!events || events.length == 0") No upcoming meetups.